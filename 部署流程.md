# TaskReminder 部署流程

> 任务扫描 + 紧迫度分级 + HTML 邮件提醒。纯 Python，仅需 PyYAML。

## 1. 系统概览

```
tasks/task_001.md   ─┐
tasks/task_002.md   ─┤  task_scanner.py     reminder.py      email_client.py
    ...              ├─ 解析 frontmatter ─→ 组稿 HTML ─→ SMTP 发送
tasks/task_NNN.md   ─┘  + 计算紧迫度                          (QQ / 163 / Gmail)
                            
phd_framework.md ─→ reminder.py 读取「本周重点计划」嵌入邮件顶部
```

## 2. 环境要求

- Python 3.9+
- `pip install pyyaml`
- 一个支持 SMTP 的邮箱（QQ / 163 / Gmail 均可）

## 3. 文件清单

| 文件 | 用途 |
|------|------|
| `task_scanner.py` | 扫描 `tasks/task_*.md`，解析 frontmatter + 计算紧迫度 |
| `reminder.py` | CLI 入口，组稿 HTML，支持 `--send` 发邮件 |
| `email_client.py` | SMTP SSL 发送封装，从 `config.json` 读配置 |
| `config.json` | SMTP 凭据（不提交 git） |
| `config.template.json` | 配置模板，复制后填入真实信息 |
| `phd_framework.md` | 可选。如果存在，邮件顶部嵌入「本周重点计划」区块 |

## 4. 必须修改的位置

部署前需要在 **3 个文件** 中修改以下位置：

### 4.1 `task_scanner.py`

| 行 | 变量 | 改为 |
|----|------|------|
| 注释 `[部署时修改]` | `TASKS_DIR` | 你的 `tasks/` 目录绝对路径 |

```python
TASKS_DIR = Path("/your/project/tasks")
```

### 4.2 `reminder.py`

| 行 | 变量 | 改为 |
|----|------|------|
| 注释 `[部署时修改]` | `FRAMEWORK_PATH` | 你的 `phd_framework.md` 路径（不需要则忽略） |

```python
FRAMEWORK_PATH = Path(__file__).parent / "phd_framework.md"
```

### 4.3 `email_client.py`

| 行 | 变量 | 改为 |
|----|------|------|
| 注释 `[部署时修改]` | `CONFIG_PATH` | 你的 `config.json` 路径（默认同目录不动即可） |

### 4.4 SMTP 配置

```bash
cp config.template.json config.json
```

编辑 `config.json`：
```json
{
    "host": "smtp.qq.com",
    "port": 465,
    "user": "your_email@qq.com",
    "password": "你的SMTP授权码",
    "to_addr": "收件地址"
}
```

常用 SMTP：
| 邮箱 | host | port |
|------|------|------|
| QQ | smtp.qq.com | 465 |
| 163 | smtp.163.com | 465 |
| Gmail | smtp.gmail.com | 465 |

## 5. 任务文件格式

每个任务文件命名 `task_NNN.md`，使用 YAML frontmatter：

```markdown
---
id: "001"
title: 你的任务标题
project: 所属项目名
status: active
priority: P1
deadline: "2026-08-15"
---

## 当前状态
- 正在做的事情
- 已完成的进展

## 下一步
- [ ] 待做事项 1
- [ ] 待做事项 2
```

字段说明：
| 字段 | 必需 | 说明 |
|------|------|------|
| id | 是 | 任务编号 |
| title | 是 | 任务名称 |
| project | 否 | 所属项目，留空则不显示 |
| status | 是 | `active`（活跃）或 `archived`（归档，不扫描） |
| priority | 是 | P1 / P2 / P3 |
| deadline | 否 | 格式 `YYYY-MM-DD`，不设则无截止提醒 |
| body | 是 | `## 当前状态` + `## 下一步`，邮件会嵌入这两段 |

## 6. 紧迫度规则

| 条件 | 等级 |
|------|------|
| P1 已过期 或 P1 15天内到期 | 🔴 high |
| P2 已过期 或 P2 7天内到期 | 🟡 medium |
| P1 无 deadline 或 P2 到期 >7天 | 🟡 medium |
| 其余 | ⚪ low |

可在 `task_scanner.py` 的 `compute_urgency()` 中调整阈值。

## 7. 发送策略

### 两层机制

1. **变动即时发送**（配合 AI agent）— agent 修改了 `task_NNN.md` 或 `phd_framework.md` 后立即执行 `python reminder.py --send`
2. **每周兜底** — cron / Task Scheduler 每周一早 8 点自动发送，确保即使没通过 agent 也能收到

### Windows（Task Scheduler）

```cmd
schtasks /Create /TN "TaskReminder" /TR "python X:\path\to\reminder.py --send" /SC WEEKLY /D MON /ST 08:00
```

### Linux / macOS（cron）

```bash
crontab -e
# 每周一早 8 点（兜底）
0 8 * * 1 cd /path/to/TaskReminder && python reminder.py --send
```

### AI agent 唤醒

在 Claude Code 等环境中说"提醒"，agent 自动执行 `python reminder.py --send`。每次任务变动 agent 也会即时发送。

## 8. 测试

```bash
# 终端预览（不发送）
python reminder.py

# 发送邮件
python reminder.py --send

# 发送到指定地址（覆盖 config.json 的 to_addr）
python reminder.py --send --to test@example.com
```

## 9. 自定义

- **邮件样式**：修改 `reminder.py` 的 `build_html()` 函数中的 HTML/CSS
- **本周重点区块**：创建 `phd_framework.md`，加 `## 本周重点计划` 段落，邮件自动读取
- **紧迫度阈值**：修改 `task_scanner.py` 的 `compute_urgency()` 中的天数

## 10. 依赖

```
PyYAML>=6.0
```

Python 标准库：`smtplib`, `email`, `json`, `argparse`, `re`, `datetime`, `pathlib`, `dataclasses`
